#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../.deno/nypm@0.6.2/node_modules/nypm/dist/cli.mjs" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../.deno/nypm@0.6.2/node_modules/nypm/dist/cli.mjs" $args
  }
  $ret=$LASTEXITCODE
} else {
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../.deno/nypm@0.6.2/node_modules/nypm/dist/cli.mjs" $args
  } else {
    & "node$exe"  "$basedir/../.deno/nypm@0.6.2/node_modules/nypm/dist/cli.mjs" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
