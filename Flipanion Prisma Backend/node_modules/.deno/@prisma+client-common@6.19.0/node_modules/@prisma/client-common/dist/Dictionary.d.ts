export interface Dictionary<T> {
    [key: string]: T;
}
export declare function keyBy<P extends PropertyKey, T extends {
    [key in P]: string;
}>(collection: readonly T[], prop: P): Dictionary<T>;
