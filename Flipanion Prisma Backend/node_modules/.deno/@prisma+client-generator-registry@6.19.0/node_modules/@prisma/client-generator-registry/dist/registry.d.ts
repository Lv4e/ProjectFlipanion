import type { Generator } from '@prisma/generator';
import type { GeneratorRegistry as IGeneratorRegistry } from '@prisma/internals';
export declare class GeneratorRegistry {
    #private;
    add(generator: Generator): void;
    addAliased(name: string, generator: Generator): void;
    toInternal(): IGeneratorRegistry;
}
