import { TypeBuilder } from './TypeBuilder';
import { Writer } from './Writer';
export declare class ArraySpread extends TypeBuilder {
    private innerType;
    constructor(innerType: TypeBuilder);
    write(writer: Writer): void;
}
export declare function arraySpread(innerType: TypeBuilder): ArraySpread;
