import { BasicBuilder } from './BasicBuilder';
type StringifyOptions = {
    indentLevel?: number;
    newLine?: 'none' | 'leading' | 'trailing' | 'both';
};
export declare function stringify(builder: BasicBuilder, { indentLevel, newLine }?: StringifyOptions): string;
export {};
