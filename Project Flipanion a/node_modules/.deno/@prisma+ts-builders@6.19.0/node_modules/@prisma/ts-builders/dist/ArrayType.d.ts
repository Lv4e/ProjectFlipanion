import { TypeBuilder } from './TypeBuilder';
import { Writer } from './Writer';
export declare class ArrayType extends TypeBuilder {
    private elementType;
    constructor(elementType: TypeBuilder);
    write(writer: Writer): void;
}
export declare function array(elementType: TypeBuilder): ArrayType;
